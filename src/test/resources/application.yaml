spring:
  profiles:
    active: test
  main:
    lazy-initialization: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 0

app:
  name: test-app
  version: 0-test
  uploads:
    directory: target/test-uploads
