server:
  port: 8080

app:
  name: Task List Manager
  version: 1.0.0
  uploads:
    directory: uploads

spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
